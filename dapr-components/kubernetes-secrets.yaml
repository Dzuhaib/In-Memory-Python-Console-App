# Task T056: Dapr Secrets Store component for Kubernetes Secrets
# Used to retrieve DATABASE_URL and OPENAI_API_KEY from K8s secrets
apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: kubernetes-secrets
  namespace: default
spec:
  type: secretstores.kubernetes
  version: v1
  metadata: []
