apiVersion: v1
kind: Secret
metadata:
  name: {{ include "todo-chatbot.secret.name" . }}
  labels:
    {{- include "todo-chatbot.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
