apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: todo-app
  labels:
    app: todo-app
    component: frontend
data:
  # Use Kubernetes DNS to resolve backend service
  NEXT_PUBLIC_API_URL: "http://backend-service:8000/api/v1"
